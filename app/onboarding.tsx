import React from 'react';
import { View, Text } from 'react-native';

const onboarding = () => {
    return (
        <View>
            <Text> Onboard </Text>
        </View>
    );
};

export default onboarding;